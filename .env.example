# Environment configuration
ENVIRONMENT=dev
REGION=us-east-1
ACCOUNT_ID=123456789012

# Project configuration
PROJECT_NAME=data-pipeline
OWNER_TAG=data-engineering

# API Configuration
API_ENDPOINT=https://jsonplaceholder.typicode.com/users
API_BATCH_SIZE=100

# Lambda Configuration
LAMBDA_TIMEOUT=300
LAMBDA_MEMORY=1024

# Data Format
OUTPUT_FORMAT=parquet

# Lake Formation
ENABLE_LAKE_FORMATION=true

# Glue Configuration
GLUE_DATABASE_NAME=data_pipeline_db
GLUE_CRAWLER_NAME=data_pipeline_crawler
CRAWLER_SCHEDULE="cron(0 2 * * ? *)"
